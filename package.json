{
  "name": "apiark",
  "private": true,
  "version": "0.2.28",
  "description": "Local-first API development platform",
  "repository": "github:berbicanes/apiark",
  "scripts": {
    "dev": "pnpm --filter @apiark/desktop dev",
    "build": "pnpm --filter @apiark/desktop build",
    "tauri": "pnpm --filter @apiark/desktop tauri",
    "lint": "pnpm --filter @apiark/desktop lint",
    "preview": "pnpm --filter @apiark/desktop preview"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  }
}
